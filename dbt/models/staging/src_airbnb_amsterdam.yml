
version: 2

sources:
    - name: staging
      database: airbnb-amsterdam
      schema: airbnb_amsterdam_june

      tables:
          - name: listings
          - name: reviews
          - name: calendar

models:
    - name: stg_calendar
      description: >
        
      columns:
          - name:

    - name: stg_listings
      description: >
        
      columns:
          - name:

    - name: stg_reviews
      description: >
        
      columns:
          - name: